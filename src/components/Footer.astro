<footer>
  <div class="footer">
    <div class="footer_left">
      <img src="../src/assets/MaksoFilm_Logo.svg" alt="maksofilm_logo" />
    </div>
    <div class="footer_center">
      <p>Send mig en besked!</p>
      <a href="/kontakt" class="arrow-button">→</a>
    </div>
    <div class="footer_right">
      <div class="social underline-animation">Instagram</div>
      <div class="social underline-animation">LinkedIn</div>
    </div>
  </div>
</footer>
<style>
  .footer {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 20px;
    padding: 20px;
    background: linear-gradient(to top, var(--secondary-color), transparent);
  }

  .footer_left {
    width: 100%;
    max-width: 330px;
  }

  .footer_center {
    display: none;
  }

  .social {
    font-family: "Manrope", sans-serif;
    color: var(--primary-color);
    font-size: 2.5rem;
    cursor: pointer;
  }


  .footer_right {
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 768px) {
    .footer {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      background: linear-gradient(to top, var(--secondary-color), transparent);
    }
    .footer_left {
      width: 180px;
      margin-left: 2rem;
    }
    .footer_center {
      display: flex;
      gap: 1.2rem;
      margin-left: 1rem;
      align-items: center;
    }
    p {
      font-family: "Manrope";
      font-size: 1.2rem;
    }
    a {
      text-decoration: none;
    }

    a:hover {
      scale: 1.05;
      background-color: rgba(147, 125, 166, 1);
    }

    .arrow-button {
      padding: 5px 35px;
      background-color: transparent;
      border: 2px solid var(--primary-color);
      color: var(--primary-color);
      font-family: "Manrope", sans-serif;
      border-radius: 5px;
      cursor: pointer;
    }

    .footer_right {
      display: flex;
      flex-direction: row;
      gap: 20px;
      margin-right: 2rem;
    }
    .social {
      font-family: "Manrope", sans-serif;
      color: var(--primary-color);
      font-size: 1.5rem;
      cursor: pointer;
    }
  }
</style>
