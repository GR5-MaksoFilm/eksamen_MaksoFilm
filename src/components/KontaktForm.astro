<form class="formular" aria-label="Kontaktformular">
  <input id="navn" name="navn_text" type="text" placeholder="Navn" />
  <input id="email" name="email" type="email" placeholder="Email" />
  <input id="tlf" name="tlf" type="tel" placeholder="Telefon" />
  <select class="select">
    <option value="" disabled selected hidden>Jeg er interesseret i...</option>
    <option value="Filmoptagelse">Filmoptagelse</option>
    <option value="Podcast">Podcast</option>
    <option value="Livestream">Livestream</option>
    <option value="Redigering">Redigering</option>
    <option value="Andet">Andet</option>
  </select>
  <label for="besked">Fortæl lidt mere om dit projekt...</label>
  <textarea id="besked" name="besked"></textarea>
  <button class="knap underline-animation" type="submit">Send</button>
</form>

<style>
  .formular {
    background-color: var(--hover-color);
    display: flex;
    flex-direction: column;
    gap: 30px;
    font-family: var(--main-font);
    width: 100%;
    max-width: 750px;
    padding: 40px;
    border-radius: 12px;
  }

  .formular input::placeholder,
  .formular textarea::placeholder {
    font-family: var(--main-font);
    color: black;
  }

  .formular input,
  .formular textarea {
    background: none;
    border: none;
    box-shadow: none;
    font-size: 1.1rem;
    border-bottom: 2px solid var(--secondary-color);
  }

  .formular label {
    font-size: 1.1rem;
    font-weight: 700;
  }

  .formular input:focus,
  .formular textarea:focus {
    outline: none;
    box-shadow: none;
    font-family: var(--main-font);
  }
  .select {
    background: none;
    font-size: 1rem;
    padding-bottom: 12px;
    font-family: var(--main-font);
    font-size: 1.1rem;
    border: none;

    border-bottom: 2px solid var(--secondary-color);
  }
  .custom-select:focus {
    outline: none;
    box-shadow: none;
    border-bottom: 2px solid var(--secondary-color);
  }

  .knap {
    background-color: var(--secondary-color);
    border: none;
    cursor: pointer;
    font-family: var(--main-font);
    font-size: 1.2rem;
    color: var(--primary-color);
    margin: auto;
    padding: 4px 48px;
  }

  @media screen and (min-width: 975px) {
    .formular {
      justify-self: start;
      place-self: center;
    }
  }
</style>
